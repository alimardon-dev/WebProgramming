It is about function scope
    c variable inside a function does not work outside the function


Searching Priority
    Inside the function
    Parameter function
    Outside the function

Arrow function can use object variable while regular function cannot use object variables like ${this.name}, ${this.age}, etc. 

